{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit",
    "test:safe": "pnpm typecheck && vitest run",
    "test:watch:safe": "pnpm typecheck && vitest watch"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.1",
    "tsx": "^4.20.5"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "dotenv": "^17.2.2",
    "hono": "^4.9.6",
    "nodemailer": "^7.0.6",
    "prisma": "^6.15.0"
  }
}